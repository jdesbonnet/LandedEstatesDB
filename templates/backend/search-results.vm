#parse ("/backend/_header.vm")

<h2>Search Results</h2>

Results: $results.size()
<ul>
#foreach ($r in $results) 
<li>
#if ($r.class=="ie.wombat.landedestates.EmployeeRecord")
ER$r.id 
 $r.description
#else 
$r
#end
</li>
#end
</ul>

#if ($nResults == 0) 
no results for search term '$q'
#end

#parse ("/backend/_footer.vm")